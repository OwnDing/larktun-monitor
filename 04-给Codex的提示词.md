# 给 Codex 的提示词（直接粘贴）

> 用法：把 `deliver/` 整个目录放进你给 Codex 的工作区，然后粘贴下面这段。
> 建议**分阶段给**，不要一次让它做完 14 个镜头——先把 A 阶段跑通再往下。

---

## A 阶段：打地基（先做这个）

```
我要用 Blender 做一支 40 秒的竖屏 3D 宣传片，风格是等距（isometric）微缩剖面屋。
工作区里有完整的制作规格，请先读这三个文件，再动手：

- 02-分镜表.md          14 个镜头的画面、时间码、动画要求
- 03-Blender制作说明.md  技术规格（坐标系、相机、渲染、配色、材质、数据流做法）
- src/blender_scaffold.py 我已经写好的脚手架，包含场景设置、等距相机、
                          材质工厂、几何助手、数据流曲线、镜头 Marker 绑定

frames/S01.png … S14.png 是每个镜头的分镜图，**构图、配色、物体位置都以它们为准**。

本阶段目标（只做这些，不要往前跑）：
1. 在 Blender 4.x 里跑通 blender_scaffold.py，确认没有 API 报错
   （我写的时候没有 Blender 环境，Principled BSDF 的接口名、EEVEE Next 的属性
    可能需要按你的实际版本修正——修正后请告诉我改了哪些）
2. 把 S02 和 S08 两个镜头做完整：客厅剖面、家具、三口之家、摄像头、
   数据流（S02 红色向上冲出屋顶 / S08 青色向下汇入录像机）
3. **S02 和 S08 必须用同一台相机、同一套家具、同一个位置**，
   唯一差别是数据流的颜色和方向。渲染出两张静帧叠图给我看，确认对齐
4. 渲染 S02 和 S08 各 90 帧，输出 PNG 序列

注意：
- 坐标转换一律用脚手架里的 sb(x,y,z)，不要手写负号
- 颜色一律用 hex_to_linear()，不要直接填 sRGB 值
- 家具用低模，不要细分、不要贴图，这个风格靠大色块和 AO
```

---

## B 阶段：其余镜头

跑通 A 之后，按下面的顺序一批批给，每批做完渲染静帧确认再继续：

```
第 1 批（房间类，复用 A 阶段的资产）：S09、S12
第 2 批（抽象/场景类）：S03、S04、S07
第 3 批（UI 类，手机屏贴图）：S05、S10、S11
第 4 批（图形类）：S01、S06、S13、S14
```

每批的提示词模板：

```
继续做 {镜号}。参考 frames/{镜号}.png 的构图和 02-分镜表.md 里该镜的
「画面 / 镜头 / 动画」三段说明。

要求：
- 复用已有的 PROP_* 资产，缺什么新建什么，新建的也放进 SHARED_PROPS
- 该镜独有的物体放进 SHOT_{镜号} collection，并按 02-分镜表.md 的起止帧
  给 collection 的 hide_render 打关键帧
- 相机：{等距主相机 / 需要透视则新建 CAM_{镜号}_PERSP，见 03 文档第 1 节}
- 做完渲染该镜的首帧、中间帧、尾帧三张静图给我看
```

---

## C 阶段：合成输出

```
14 个镜头都做完之后：
1. 用 timeline marker 把 14 台相机绑好，确认一次渲染能出完整 1200 帧
2. 渲染全片 PNG 序列到 render/all/
3. 用 ffmpeg 合成：
   ffmpeg -r 30 -i render/all/%04d.png -c:v libx264 -crf 18 -pix_fmt yuv420p larktun_01.mp4
4. 把 .blend 文件整理干净（删掉未使用的数据块，Pack 外部资源）

字幕不要在 Blender 里烧进去——我在剪辑软件里加。
```

---

## 给 Codex 的几条硬约束（每个阶段都带上）

```
硬约束：
1. 绝对不要出现任何真实厂商的 logo、UI 截图、可识别的建筑外形或品牌名。
   机房塔、付费墙都必须是原创的抽象形象。
2. 全片只有两种有语义的颜色：红 #FF4B4B = 数据离开你/要花钱；
   青 #2FE0C8 = 数据留在你这/你掌控。不要自己加第三种。
3. 等距相机的 rotation_euler 固定 (54.7356°, 0, 45°)，不要改成 60°。
   要推拉请给 ortho_scale 打关键帧，不要移动相机位置。
4. S02 和 S08 是全片的记忆锚点，必须逐像素对齐。
5. 渲染引擎用 EEVEE，不要用 Cycles——这个风格看不出差别，但慢 20 倍。
```

---

## 需要我这边先确认的两件事

在让 Codex 开工之前，最好先定下来，否则后面要返工：

1. **品牌色**：分镜里的青色 `#2FE0C8` 是占位。换成云雀通正色后，
   改 `src/iso.py` 里 `P['brand']` 一个值 → 重跑 `python3 render.py` → 分镜图全套更新；
   Blender 侧改 `PALETTE['brand']` 一个值。
2. **S05 的价格数字**：现在是 `¥XX /月`。填上真实市场价，冲击力会强很多。
