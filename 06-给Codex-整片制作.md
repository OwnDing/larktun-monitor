# 云雀通宣传片 01 · 整片制作（一次做完）

在 vision 仓库里新建分支 `film/v1`，每个阶段提交一次。不要在 main 上直接改。

先读这几份（已有）：
- `02-分镜表.md` 14 镜的画面 / 镜头 / 动画 / 音效
- `03-Blender制作说明.md` 数据流做法、材质、渲染设置
- `05-户型与场景说明.md` 户型、坐标系、六个机位
- `renders/v1_framing/framing_report.json` 上一轮 V1 构图验证的方法和产出
- `frames/S01–S14.png` 分镜图（v1 画面构思，构图和配色仍然有效）

主工程 `Larktun_Home.blend`。先复制成 `Larktun_Film.blend` 再改，房屋主工程保持不动。

---

## 已经定了的，不要重新讨论

1. **V1 机位** = Z 225°（西北上空）· `ortho_scale 18` · `look_at (6.0, 4.6, 1.3)` · 1080×1920。
   相机 `rotation_euler = (54.7356°, 0°, 225°)`，直接赋值，不要用 to_track_quat 推算。
   `layout.json` 里原来写的 45° / 9.5 是错的，这一轮顺手改掉。
2. **D1 录像机 + D3 路由器从书房搬到客厅电视柜**（R07 电视柜 x 7.92, y 5.05, 0.42×1.90×0.45）。
   D2 NAS 留在书房书柜。D4 弱电箱、D5 AP 面板位置不变。
   原因：书房在东北角、客厅在中南，一个紧画幅装不下两头；搬到电视柜后 S08 的三条青流
   能和一家人同框，S02 / S08 的严格同构图才保得住。
3. **S01 用独立相机 V1a**：同方位 225°，`look_at` 锁 C1 (8.34, 3.52, 2.35)，
   `ortho_scale` 关键帧 1.6 → 18（ease out）。C1 在 V1 里只有 29×34 px，撑不起 S01 的钩子。
4. **颜色只有两种语义**：红 `#FF4B4B` = 数据离开你；青 `#2FE0C8` = 数据留在你这。
   金色只用在 S06 的钱。不要自己加第三种有意义的颜色。
5. **不出现任何真实厂商的 logo、UI、可识别建筑或品牌名。** 机房体块必须是原创抽象形象。

---

## 阶段 A · 场景修改

### A1 设备搬家
- D1 NVR、D3 路由器移入客厅电视柜（放在柜内开放层，正面朝 +y 南，LED 可见）
- D2 NAS 留在书房，位置不变
- 保留所有 `larktun_role` 自定义属性

### A2 网线可见（重要）
现在 `cable_route_guide` 是 `hide_render=True`。S08「录像存回你自己家」最有说服力的地方，
就是观众能看见三根线从摄像头汇到录像机——这是物理证据，比字幕管用。

把下面这几段做成**可见的实体线缆**（Curve + bevel_depth 0.003，深灰哑光）：
- C1 (8.34, 3.52, 2.35) → 沿客厅东墙下行 → 电视柜 D1
- C2 (8.34, 0.52, 2.30) → 沿玄关东墙 → 穿墙至客厅 → 电视柜 D1
- C3 (10.62, 4.88, 2.20) → 沿儿童房北墙 → 走廊 → 客厅 → 电视柜 D1
- D1 ↔ D3 之间的短跳线，D3 → D4 弱电箱的上联线
线缆要有自然垂坠（不要直角折线），沿墙走线用线槽或贴踢脚线上沿。

### A3 修掉「一眼看出是生成的」的破绽
- **客厅和儿童房挂的是同一幅日落山景画** —— 这是资产复用最明显的信号。
  儿童房换成孩子的蜡笔涂鸦 / 照片墙；主卧再换一幅不同的。三个房间三幅不同的画。
- **儿童房没有孩子的痕迹**：加散落的玩具、书包、贴纸、矮书架上的绘本、床边的毛绒玩具。
  S12 全靠这间房打动人。
- **手机屏是黑的**：确认茶几上那台手机的屏幕是**独立材质槽**，后面要贴 UI 序列帧。

### A4 生活痕迹（每处至少一件）
玄关地上一双没归位的鞋、沙发扶手搭着的外套、厨房台面的调料瓶和洗洁精、
阳台晾着的衣服、餐桌上没收的杯子。已有的账单 / 遥控器 / 水杯保留。

### A5 不要摆太齐
给餐椅、抱枕、地毯、桌面小物加 ±3° 随机 Z 旋转和 ±15 mm 随机位移。写成脚本批量做。

### A6 人物（最可能翻车的一环，认真看）
**质量底线**：必须有分开的四肢和头颈、衣服是有体积的几何（不是贴在身上的贴图）、
坐姿站姿是真实的关节角度。**不能是胶囊人、球头棍身、或圆滚滚的低多边形小人。**

在 V1 / ortho_scale 18 下，1 m ≈ 107 px，成年人约 188 px 高——能看清体型和衣服，
看不清五官。所以**不需要做表情和精细面部**。

按下面顺序试，**用哪条路要写进报告**：
1. MakeHuman（开源、免登录下载、有 Blender 导入插件）—— 首选，最可自动化
2. Mixamo（免费角色 + 动作，需要 Adobe 账号）
3. Blender 社区免费人物资产（CC0）

角色（`layout.json` 的 `people` 字段）：
P1 男主人 1.76 m / P2 女主人 1.63 m / P3 孩子 1.12 m（6 岁左右）/ P4 猫（可选）

**站位要重新核**：房屋工程里沙发已经从 2.45 m 缩到 2.18 m 并移到 y=5.93，
`layout.json` 的 blocking 坐标是按旧家具给的，逐个核对，不要让人站进沙发里。

**如果三条路都走不通**：不要退回简化小人。改用**背光剪影**方案——
S02 / S08 里一家人坐在餐桌旁，逆着北窗的光，只见轮廓不见细节，
镜头重心落在数据流和空间上。这个方案是可接受的，但必须在报告里写明用了它。

---

## 阶段 B · 相机

V1a 和 V1 已经定了（见上）。**V2–V6 用上一轮验 V1 的同一套方法自己验**：
每台相机跑四个方位（Z 45 / 135 / 225 / 315）× 三档 `ortho_scale`，
输出 `world_to_camera_view` 的可见性报告，自己选，选的理由写进报告。

每台相机要保证在画幅内且**参与渲染**（不被剖切隐藏）的关键物体：

| 相机 | 用于 | 必须在画面内 |
|---|---|---|
| V1a | S01 | C1 摄像头（拉开后还要有客厅） |
| V1 | S02 S07 S08 S11 S13 S14 | C1、电视、电视柜（D1/D3）、餐桌、沙发 |
| V2 | S03 S04 | 整户屋顶、三台摄像头的位置 |
| V3 | S06 | 餐桌、账单、手机 |
| V4 | S05 S10 | 茶几上的手机（屏幕要占画面 1/2 以上） |
| V5 | S12 | 儿童床、C3、夜灯 |
| V6 | S09 | 南窗 / 阳台推拉门、电视柜方向 |

**剖切规则**：不要用 1.15 m 均匀剖切，它会把 C1（z=2.35）、电视、挂画全切掉。
用完整墙体，每个方位只隐藏位于相机与主体之间的墙和房间，远侧墙保留全高 2.8 m，天花板关闭。

---

## 阶段 C · 三套灯光

做成三个 **View Layer** 或三个 Scene 共享同一套模型，不要在一套灯上来回调。

现在的 1800W 大柔光 + 午后阳光 + World 0.36 是白天样板间，片子一帧都用不了——
红流和青流是自发光的，**得有暗底才发得出来**。

### 暗光 LOOK_DARK（S01 S03 S04 S07 S14）
- Key 降到 60–90 W；World 色 `#05070E` strength 0.08
- 主要亮度来自自发光元素（LED、数据流、窗外微光）
- 曝光 −0.6
- **验收：≥70% 的像素落在明度 0–0.25 区间**

### 傍晚暖光 LOOK_DUSK（S02 S05 S06 S08 S09 S10 S11 S13）
- Key 220 W / 3400 K 从西窗进；室内暖灯 40–70 W / 2700 K；World 0.12
- 曝光 −0.2
- **验收：中位明度 0.30–0.45，家具细节仍可读**

### 夜光 LOOK_NIGHT（S12）
- Key 45 W 冷蓝 `#3A5580` 从窗外；儿童房夜灯 Point 25 W `#FFC061` radius 0.15；World 0.06
- **验收：只有夜灯周围一圈暖，其余冷暗**

沙发材质 `#667D88` 改成 `#3E4C6D`——它现在是灰青色，离品牌青太近，青色数据流流过会糊。

---

## 阶段 D · 数据流系统

三种做法，`03-Blender制作说明.md` 第 6 节有完整描述，照做：

1. **光管**（S02 红流、S08 青流、S09 隧道）：Bezier Curve + `bevel_depth`（流 0.06 / 隧道 0.18），
   Emission 材质里 Gradient → Wave → ColorRamp 接 Emission Strength，
   Mapping 的 Location.X 挂 driver `frame * speed / fps` 做无限流动。
   隧道生长用 `bevel_factor_end` 关键帧 0 → 1，EASE_OUT。
2. **数据包**（菱形小方块）：Geometry Nodes，Resample Curve → Instance on Points → 沿切线偏移。
3. **脉冲环**（S07 切断、S01 红环）：Torus + Emission，`scale` 和 Emission Strength 同时打关键帧，
   错开 4 帧复制 3–4 份。

发光强度：红 14–18，青 12–16。EEVEE 里自发光不产生真实光照，
要让数据流照亮周围，在路径上放几盏低强度 Point light 跟着动。

---

## 阶段 E · 14 镜时间轴（30 fps，共 1200 帧）

每镜一台相机，用 timeline marker + Ctrl+B 绑定，一次渲染出全片。

| 镜 | 帧 | 相机 | 灯光 | 动画 |
|---|---|---|---|---|
| S01 | 1–90 | V1a | DARK | C1 红点亮度 0.2→1.0→0.2 循环周期 42 帧；同心红环扩散 ×2；`ortho_scale` 1.6→18 ease-out |
| S02 | 91–180 | V1 | DUSK | 红流从 C1 射出穿天花板出画右上；数据包循环流动 1.2 单位/秒；人物 idle（头部 2°） |
| S03 | 181–270 | V2 | DARK | 三条红流从屋顶汇出画面，出画后接远处无品牌暗色体块；体块红灯带自下而上滚动 |
| S04 | 271–360 | V2 | DARK | `ortho_scale` 收紧推向体块；体块表面浮出发光小窗格，其中一格高亮成本户客厅缩略图 + 红框脉冲 ×2 |
| S05 | 361–450 | V4 | DUSK | 手机屏播放付费墙 UI 序列；0.4s 时间轴灰掉 + 锁落下 → 1.2s 付费墙滑入 → 2.0s 点击涟漪弹回 |
| S06 | 451–540 | V3 | DUSK | 餐桌俯拍：账单摊开 + 手机扣款推送；金额用叠加图形表达（合成阶段） |
| S07 | 541–630 | V2 | DARK | 帧 541–547 青色脉冲炸开截断红流；548–560 上半段碎裂；561–630 碎片飘散淡出。**整个切断 0.2 秒内完成** |
| S08 | 631–720 | V1 | DUSK | **与 S02 严格同机位**。红变青、向上变向下，三条青流沿网线汇入电视柜 D1；硬盘 LED 依次点亮；全屋淡青光晕呼吸 |
| S09 | 721–810 | V6 | DUSK | 青色隧道从电视柜生长穿出南窗伸向远方，`bevel_factor_end` 0→1 用 36 帧完成；光芯流动 |
| S10 | 811–900 | V4 | DUSK | 手机屏播放云雀通 App UI 序列；实时画面从中心 0.5s 展开；延迟数字 --→18ms |
| S11 | 901–990 | V1 | DUSK | 基板用 S02 / S08 的渲染帧，上下分屏 + 路径线叠加（合成阶段做）。上路径走 1.2s（慢、绕），下路径 0.5s（快、短） |
| S12 | 991–1080 | V5 | NIGHT | 夜灯光晕缓慢呼吸；青线流动光点每 24 帧往返一次；孩子熟睡 |
| S13 | 1081–1140 | V1 静帧 | DUSK | 三张卡片自下而上依次弹入，间隔 6 帧，overshoot 6%（合成阶段做） |
| S14 | 1141–1200 | V1 | DARK | 客厅画面渐暗 → 雀鸟 Logo 浮出 → 定格 1.2s → 字幕淡入 → 最后 0.5s 黑场 |

**S02 和 S08 必须逐帧对齐**，渲完把两帧叠图对比，写进报告。这是全片唯一的记忆锚点。

---

## 阶段 F · 手机 UI

不要在 Blender 里建 UI。`src/scenes_a.py` 的 `s05()` 和 `src/scenes_b.py` 的 `s10()` 里，
变量 `inner` 就是完整的手机屏 SVG（付费墙 / 云雀通 App）。

把它们抠出来单独渲染成 1080×2200 的 PNG（动效部分出序列帧），
贴到手机屏面片上做 Emission 贴图，strength 2.5。

S05 的价格现在写的是 `¥ XX /月` —— 保持占位，不要自己编数字。

---

## 阶段 G · 渲染

**先用 EEVEE Next 出全片**，1200 帧。这是风格化等距片，EEVEE 足够，比 Cycles 快 20–50 倍。

```
engine        = BLENDER_EEVEE_NEXT
resolution    = 1080 × 1920 @ 100%
fps           = 30，帧范围 1–1200
gtao          = on, distance 0.35
taa_render_samples = 64
raytracing    = on（如果该版本有）
view_transform= AgX
输出          = PNG 序列 16-bit RGBA → out/frames/####.png
```

**开渲之前先估时并报出来。** 如果预估超过 2 小时，先停下告诉我，不要闷头渲一晚上。

---

## 阶段 H · 字幕与合成

字幕**不要烧进 Blender**，在 ffmpeg 阶段做，这样改文案不用重渲。

样式：思源黑体 Heavy / Noto Sans CJK SC Black，74 px（1080 宽基准），白色 + 14 px 深色描边，
居中，基线在 y=1560。单行不超过 9 个汉字。

| 镜 | 字幕 | 角标 |
|---|---|---|
| S01 | 你家客厅的画面 | — |
| S02 | 每一秒 / 都在离开你家 | — |
| S03 | 最后存在 / 谁的硬盘上？ | — |
| S04 | 别人的机房里 / 有一格是你家 | — |
| S05 | 想回看昨天？/ 先交月费 | — |
| S06 | ×3 台摄像头 | 云存储月费 |
| S07 | 可它 / 本来就不用出门 | 转折 |
| S08 | 录像 / 存回你自己家 | — |
| S09 | 云雀通 / 只打一条回家的路 | — |
| S10 | 点开，就是家里 | — |
| S11 | 同一个画面 / 两条完全不同的路 | — |
| S12 | 画面 / 从没离开过你 | — |
| S13 | 无需公网 IP · 免端口映射 · 全平台客户端（三张卡片） | 装完就能用 |
| S14 | 云雀通 Larktun / 把回家的路，握在自己手里 / docs.larktun.com | — |

输出两版：
```bash
# 无字幕版（给剪辑用）
ffmpeg -r 30 -i out/frames/%04d.png -c:v libx264 -crf 18 -pix_fmt yuv420p out/larktun_01_clean.mp4
# 带字幕版
ffmpeg -i out/larktun_01_clean.mp4 -vf "ass=out/subtitles.ass" -c:v libx264 -crf 18 -pix_fmt yuv420p out/larktun_01_sub.mp4
```

**不要做音频。** 输出无声视频，另外写一份 `out/audio_cue_sheet.md`，
按帧列出音效点（内容抄 `01-创意与分镜脚本.md` 第三节），我另外找人做声音。
特别标出 **帧 541 起 BGM 全部骤停 9 帧（0.3 秒）**，这是全片最重要的空拍。

---

## 每个阶段的自检（过不了就停下报告，不要硬推到下一阶段）

- **Gate A**：场景改完，渲 V1 / S18 一帧。确认 **D1 在画面内**、电视柜可见、三条网线可见。
  出一份和上一轮同格式的 `framing_report.json`。
- **Gate B**：V1a 和 V2–V6 各出一帧 + 关键物体可见性报告（in_frame 和 render_enabled 分开判）。
- **Gate C**：三套灯各出一帧 + 明度直方图。DARK 的 0–0.25 占比、DUSK 的中位明度要达标。
- **Gate D**：数据流做完，出 S02 和 S08 各三帧（首 / 中 / 尾），确认红流青流方向对、发光够。
- **Gate E**：14 镜各出首帧，拼成一张 contact sheet，和 `frames/S01–S14.png` 并排对照。
- **Gate F**：全片渲完，抽 24 帧（每 50 帧一张）做 contact sheet。

Gate A 和 Gate C 是硬门槛：D1 不在画面内、或者 DARK 不够暗，后面全都白做。

---

## 硬约束

1. 在分支 `film/v1` 上做，每阶段提交一次。`Larktun_Home.blend` 不要改，复制成 `Larktun_Film.blend`。
2. 等距相机 `rotation_euler` 的 Z 只能取 45 / 135 / 225 / 315，X 固定 54.7356°。
   要推拉给 `ortho_scale` 打关键帧，不要移动相机位置。
3. 全片只有红青两种语义色，金色只用在 S06。
4. 不出现任何真实厂商 logo、UI、可识别建筑、品牌名。
5. S05 的 `¥ XX` 保持占位，不要编造价格。
6. 品牌青 `#2FE0C8` 和 S14 的雀鸟 Logo 都是占位设计，保留原样，不要自行"优化"。
7. 人物达不到质量底线就走背光剪影方案，**不要退回简化小人**，并在报告里写明。
8. 不做音频。

## 最后交付

```
out/larktun_01_clean.mp4      无字幕成片
out/larktun_01_sub.mp4        带字幕成片
out/frames/                   PNG 序列
out/subtitles.ass             字幕文件
out/audio_cue_sheet.md        帧级音效表
Larktun_Film.blend            影片工程
scene/layout.json             更新 V1–V6 最终值、设备新坐标
PRODUCTION_REPORT.md          各 Gate 的产出、人物走了哪条路、
                              所有偏离本文档的地方及原因、已知问题清单
```

`PRODUCTION_REPORT.md` 里**如实写下哪些没做到、哪些打了折扣**。
做不到不丢人，把做不到的地方藏起来才麻烦。
