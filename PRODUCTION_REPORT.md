# 云雀通 01 · 制作报告

状态：**阶段 A–I 全部完成，Gate A–F 及 G/H/I 交付检查通过。** 已交付：
- 两版 40 秒无声成片，以及同画面的两版有声成片
- 1200 张原始帧
- 可编辑影片工程
- 独立字幕和帧级音效表
- 原创音轨及分轨

下文保留各阶段证据与实际折扣，不将早期预览冒充最终帧。

制作分支：`film/v1`。源模型：`Larktun_Home.blend`；影片模型：`Larktun_Film.blend`。
源模型 SHA-256：`15fb9bde761c3eeddc9821581d3d45e278be4411659a840cc8c564a0ae229efb`。

## 指令优先级与冲突处理

以本次用户指令及 `06-给Codex-整片制作.md` 为准。旧文档中的 V1=45°/9.5、统一白天光、简化人物、替换品牌占位色、填入真实价格等已被新要求取代。
S07 使用新版时间轴指定的 V2；其他固定条件为 30 fps、1200 帧、1080×1920、无音频。阶段 I 按后续用户指令追加音轨，见 I 节。

## A · 场景修改 / Gate A：PASS

- D1 NVR、D3 路由器移到客厅电视柜开放层，正面朝图纸 +y 南。原有 `larktun_role` 保留；D2 NAS 原变换逐项一致，留在书房。
- 原电视柜实心体块替换为同尺寸的开放柜体、隔板、顶板和支脚。固定西北机位看不到朝南的原正面 LED，因此增加了**实体顶沿状态复示灯**，没有改变设备朝向。这是新增的可读性细节。
- 五条实体线缆均为 Bezier Curve，`bevel_depth=0.003 m`，深灰哑光，绕门楣和实墙边走线；包含三路摄像头至 D1、D1–D3 短跳线、D3–D4 上联。
- 儿童画替换重复挂画；主卧增加独立植物线描。增加书包、几何肩带、绘本、毛绒兔与儿童脚踏。原有玩具、晾衣、鞋、杯子、账单、遥控器和厨房小物保留。
- 手机屏幕拥有独立材质槽。餐椅、抱枕、地毯、杯子等按完整组件批量偏移，随机种子和实际偏移记录于 `out/gates/A/jitter.json`。
- V1 已写回 `scene/layout.json`：Z225、X54.7356、scale18、look_at=(6,4.6,1.3)。D1/D3 新坐标、实体网线路径和人物站位同步更新。

### 人物路线与质量范围

使用第一条路线 **MakeHuman**：官方核心人体、亚洲成人/儿童形态、139 组骨骼权重，配合原创建模的有厚度棉上衣和长裤。按身高 1.76 / 1.63 / 1.12 m 生成；大腿、膝、踝、肩、肘、腕使用人体骨架变换，三人朝向餐桌。
本阶段没有使用胶囊、球头棍身或简化低多边形人；未启用剪影回退。面部没有精细表情，头发为简化短发网格，衣服没有布料动力学；这些细节按等距远景处理。

素材源：[MakeHuman 官方仓库](https://github.com/makehumancommunity/makehuman)，固定版本 `a8bc2d54ff0ac92e78ff71431b1023eda42bf482`。
核心图形资产采用 CC0：[官方许可说明](https://static.makehumancommunity.org/about/license.html)。逐文件来源、SHA-256 和许可证保存在 `assets/characters/provenance.json` 及 `LICENSE.ASSETS.md`。

### Gate A 证据与实际限制

- 原图：`out/gates/A/V1_Z225_S18.png`，1080×1920。
- 报告：`out/gates/A/framing_report.json`。`in_frame` 与 `render_enabled` 分开记录；包围盒来自实际评估几何。
- D1 在画幅内，投影约 48×32 px，开放柜体与设备可辨认。
- 三路线路分别约 **77.0% / 90.2% / 55.6%** 的 Bezier 采样点可直视；每段曲线 25 个采样点逐一射线检查。三条线路均有可见的墙面段和柜边汇合段。
- **并非整条线无遮挡**：C2/C3 的穿墙部分、部分隔墙后的路段和柜内部分被真实几何遮挡。报告逐条列出遮挡物；没有把“入框”写成“无遮挡”。
- `cabinet_cables_detail_3x.png` 和 `characters_detail_3x.png` 为原帧放大裁片，仅用于检查，不替代成片画幅。
- Gate A 使用原有白天光检查几何；它不是最终影片灯光，三套正式灯光在 C 阶段建立。

## B · 相机

Gate B：PASS。V2–V6 共 60 张 1080×1920 实际渲染候选，另有 V1a 起止帧。证据在 `out/gates/B/`，总览为 `selected_contact_sheet.png`，所有候选和实际投影见 `framing_report.json`。

| 相机 | Z / scale | 选择依据 |
|---|---|---|
| V1a | 225 / 1.6 → 18 | 固定看点 C1；拉开后保留电视、沙发与客厅 |
| V2 | 225 / 42 | 整户与三台摄像头入框；原创远端体块在房屋上方 |
| V3 | 225 / 3.8 | 餐桌、账单、手机同框 |
| V4 | 225 / 0.18 | 手机正对镜头，屏幕面积占全幅 53.4% |
| V5 | 45 / 7.5 | 保留承载 C3 的北墙，同时包含儿童床和夜灯 |
| V6 | 225 / 15 | 看得到电视柜开放面与完整南侧客厅推拉门 |

所有实际主体的 `in_frame` 与 `render_enabled` 分开检查并通过。V2 的屋顶范围以关闭的天花板几何测量，天花板本身不参与渲染，这是剖切规则要求。南窗报告修正了名称混淆：正确对象为“客厅推拉门”的四片玻璃，北侧“阳台推拉门”不能替代它。隐藏对象使用原始世界变换计算范围，避免失效的评估缓存。

手机特写复用原手机几何，增加茶几上的倾斜支架；S06 另有餐桌上的同款手机和账单。两者仅在对应镜头启用，S02/S08 的平放手机保持一致。支架穿模已修正。近景检查后提升衣服肩颈覆盖，头发改用连续头皮网格，避免原 helper 网格分瓣。

本阶段仍以源白天光看构图。Blender 5.2.1 的 Eevee 引擎标识为 `BLENDER_EEVEE`，旧的 `BLENDER_EEVEE_NEXT` 在此版本无效；正式制作采用当前 Eevee。试片出现旧灯组阴影缓冲不足警告，因此这些图不作为灯光验收。C 阶段将关闭旧灯组，单独验收三套灯光。

## C · 三套 View Layer 灯光

Gate C：PASS。三个独立 View Layer 各有固定灯组及独立 World override；原白天灯组全部排除。切换层时只读取该层固定曝光值，不在同一套灯上改参数。

| LOOK | 实测结果 | 验收 |
|---|---|---|
| DARK | 0–0.25 像素占比 **98.80%** | ≥70%，通过；保留微弱冷色空间轮廓 |
| DUSK | 全幅中位明度 **0.322** | 0.30–0.45，通过；家具可读 |
| NIGHT | 低明度像素占比 92.55% | 夜灯暖池、窗侧冷色轮廓，床体可读 |

测量使用完整 1080×1920 帧，不裁切、不排除背景。显示 RGB 的 Rec.709 加权明度，256 档直方图和原帧在 `out/gates/C/`；失败的前两轮保存在 `iteration_01` / `iteration_02`。全部实际灯位、功率、World 和曝光见 `lighting_settings.json`。

DARK key 80 W / World #05070E ×0.08 / 曝光−0.6；DUSK key 220 W 3400 K / 室内 40–65 W 2700 K / World 0.12 / 曝光−0.2；NIGHT key 45 W #3A5580 / Point 25 W #FFC061、radius 0.15 / World 0.06。沙发布料改为 #3E4C6D，保留织物程序纹理。

调整及折扣：DUSK World 使用中性白；屋外展示底板由原奶油色改为中性白，以达到全画幅中位明度要求，房间地板材质没有因此提亮。夜灯 Point 关闭阴影，避免原灯罩挡住全部出光；冷窗光放在南窗内侧。DARK key 移近剖开一侧，避免房屋完全沉黑。首次发现后创建的灯组被旧 Layer 默认纳入，已修复所有层的互斥关系。正式预览不再出现旧灯组的 shadow-buffer-full 警告。

## D · 数据流系统

Gate D：PASS。`out/gates/D/` 含 S02、S08 首/中/尾共六张实际渲染帧、拼版、相机变换及 SHA-256。六帧数据包位置不同；红流从 C1 朝画面右上离家，青流沿 C1/C2/C3 实体线缆方向汇入 D1。

光管使用 Bezier + 0.06 m，隧道 0.18 m；Gradient → Wave → ColorRamp 驱动发光强度，Mapping X driver 为 `frame*1.2/30`。数据包为 Geometry Nodes：Resample Curve → 按弧长比例循环采样位置及切线 → 菱形实例。速度 1.2 m/s；红最大 16、青最大 14。每路径三盏低强度 Point 随数据流移动，关闭阴影；合成器使用适度 Fog Glow。

S01/S07 均建立四个 Torus，错开四帧，缩放与发光关键帧；S01 两轮扩散。实际时间轴控制在 E 阶段连接。

限制：完整远侧墙仍会遮住 C3 和走廊的一部分路径；三路下行在 D1 前靠得很近，发光会合并，这是实际线缆间距和规定 0.06 m 管半径共同造成。没有把它们改成穿透墙体的屏幕线。AgX 使高发光核心趋向浅色/白色，材质定义仍严格为指定红青。

## E · 14 镜时间轴

Gate E：PASS。14 个 timeline marker 绑定指定相机，帧范围 1–1200 / 30 fps。首帧与原分镜并排对照：`out/gates/E/storyboard_comparison.png`；首帧报告与额外动作中间帧在同目录。

S01 以独立 V1a 的 90 个正交尺度关键帧实现 cubic ease-out，1.6→18；C1 的实体 LED 移到壳体可见上侧，脉冲环对准该 LED，避免原指示灯被机身挡住。S04 只改变 V2 的 ortho_scale 42→12。所有相机位置及 XYZ 角度保持不动。

**S02/S08 全 90 对相机和完整骨骼姿态矩阵逐项比较通过**，见 `S02_S08_camera_alignment.json`；每对使用同一个 V1 对象、相同尺度、相同动作相位。渲后 90 张叠图已在 G 完成。

S03/S04 原创抽象体块的窗格自下而上滚动，其中一格展示本户实际画面；低强度深灰蓝自发光辅助读出远端体块轮廓。S07 第 547 帧结束上半段截断，第 548 帧启用碎片，560 后漂移缩小并熄灭。S08 硬盘灯逐个点亮并有轻微全屋青色反光。S09 外管 0.18 m 采用透明包络，内部流动光芯 0.035 m，36 帧生长穿过南窗。外壳透明度 0.13、发光上限 1.2，内部芯线保持青色上限 14，这是为避免粗光管盖住房屋而做的表现调整。S06 账单是两片带实体墨线的铰接纸面展开。

S12 使用同一 MakeHuman 儿童模型侧卧、屈膝，覆盖有厚度的小被；一颗青色小光点在半径 0.006 m 的弱光线段上每 24 帧往返，夜灯在 23–25 W 之间缓慢呼吸。该夜景没有使用白天镜头那样粗的管线和大数据包。

本阶段首帧仍明确缺少 F/H 负责的元素：手机 UI、S06 金额图形、S11 分屏、S13 卡片、S14 雀鸟与收尾淡出。它们不冒充已经出现在本阶段 PNG 中。可编辑骨骼、纸面、脉冲和相机关键帧保存在 blend；逐镜隐藏与 View Layer 选择由 `film/motion.py` 和工程内 `RUN_FILM_TIMELINE.py` 控制。

## F · 手机 UI

阶段 F：PASS。S05/S10 各 90 张 1080×2200 PNG，使用独立手机屏幕材质、Emission Strength 2.5。灰掉时间轴、落锁、付费墙滑入、点击弹回、实时卡片 15 帧展开和 `-- → 18ms` 均以序列图实现；S06 有独立扣费通知，金额始终为 `¥ XX`。

`out/ui/*_original.svg` 保存源 `inner`；`out/ui/S05.svg` / `S10.svg` 是动画副本。修正源 SVG 无效的 `text-anchor="center"` 为 `middle`，否则文字会溢出右边。S10 使用本户真实渲染替换原示意缩略图，LIVE 标记统一青色。原始雀鸟完整提取到 `S14_bird_original.svg`，路径、形状和色值未改。

UI 字体使用 Noto Sans CJK SC Regular/Bold/Black；字幕专用 Black 保留在 `assets/fonts/`。字体来自 [Noto 官方仓库](https://github.com/notofonts/noto-cjk)，OFL 许可证及固定提交/SHA 见 `assets/fonts/provenance.json`。UI 八张实际屏幕预览和拼版在 `out/gates/F/`。各 UI 材质保留 fake user，关闭再打开工程时不会丢失未激活的界面。

## G · 渲染

**阶段 G：PASS，1200 帧完整交付。** 开渲前每个镜头用最终 1080×1920、64 采样、raytracing、AgX、16-bit RGBA 实渲一帧，共 14 帧；包含帧状态和骨骼更新耗时。按镜长加权为 **100.86 分钟**，增加 25% 波动余量后为 **126.08 分钟（约 2 小时 6 分钟）**。

估时超过用户的两小时门槛，已在开渲前请求确认，用户明确回答“按原规格继续渲染”；没有降低采样、分辨率或关闭光线追踪。详见 `out/gates/G/render_estimate.json`、14 张 benchmark PNG 及拼版。主渲染累计 5875.42 秒，96 帧收尾补渲 468.71 秒，程序累计 **6344.13 秒 / 105.74 分钟**，包含 S04 返工；不包含前期试片、人工验看和后期合成。
PNG 序列为本地生成交付物，Git 提交渲染脚本、清单和验收证据；`out/frames/*.png` 不进入版本历史。

正式渲染到 S04 时，时间函数检查发现红框会产生四次脉冲，要求为两次。已停止该轮渲染，保留经过 SHA-256 核对的 1–270 帧，将 271 起受影响的 S04 帧移入诊断目录；周期修正为 45 帧后重新渲染 S04 并续跑后续镜头。渲染分辨率、采样、raytracing 均未改变。详情见 `out/gates/G/S04_pulse_correction.json`，日志保留初次与续跑两份；最终耗时包含这次返工。

完整 S05 与字幕叠加时发现按钮被固定字幕基线遮住。动画副本中的付费卡片上移 48 SVG 单位，底部辅助句移到按钮上方并改为 14 SVG 单位；原始 SVG、所有文案、`¥ XX` 和相机参数保持。361–450 共 90 帧已按原规格补渲，最终第 424 帧确认按钮、辅助句和叙事字幕可同时完整阅读。证据在 `out/gates/G/S05_caption_safezone/`；F 的 UI 预览是此前阶段快照，最新序列哈希以 G 的 `final_corrections/patch_report.json` 为准。

S07 前六帧的路径附光筛选曾误纳入其他镜头的青色附光，造成屋顶提前返青。已修正镜头筛选并完成 541–546 帧补渲；三套基础灯组的参数保持不变。96 帧的相机实际变换、正交尺度与原帧逐项相同；前后哈希、备份和耗时在 `out/gates/G/final_corrections/`。

`out/gates/G/render_validation.json` 确认 1200 帧均为 1080×1920、16-bit RGBA，逐文件 CRC 和 SHA-256 验证通过；没有缺帧或多余 PNG，所有相机位置固定、Euler 角符合限定。房屋源工程 SHA-256 与 A 阶段一致。每 50 帧抽一张的 24 帧原始拼版位于 `out/gates/F/fullfilm_contact_sheet.png`，按文件中的 Gate F 要求在全片渲染后补齐。

**S02/S08 实渲 90 对逐帧同机位：PASS。** 实际相机对象、位置、Euler、scale、View Layer 全部逐项相同；V1 水平覆盖 10.125 m，垂直 18 m。每对均生成 50% 叠图，保存在 `out/gates/G/alignment/`，逐帧报告为 `S02_S08_rendered_alignment.json`。首/中/尾拼版可见墙、电视、餐桌、沙发和人物对齐；流动方向、颜色及局部附光本来不同，不将像素差异当成机位误差。

Blender 在正常退出后报告一块 0.062584 MB 内存未释放；主渲染和补渲进程退出码均为 0，全部帧已通过独立校验。原始日志保留在 `out/logs/`，没有隐藏这条退出警告。

交付数据终检同步了 `layout.json` 的全局默认 Euler、V2–V6 方位文字、V2 的 S07 归属、旧书房网线平面路由，以及 S12 的真实骨架根位置和姿态。原有示意线与人物备注保留在 `source_*` 字段。当前 `cables` 对应实际影片线缆的平面投影，完整高度路径仍在 `physical_cables`。

## H · 字幕、合成与声音表

**阶段 H：PASS。** 最终输出 `out/larktun_01_clean.mp4`（无叙事字幕）及 `out/larktun_01_sub.mp4`（带叙事字幕）。两者均为 1080×1920、30 fps、1200 帧、40.000 秒、H.264 / CRF18 / yuv420p / BT.709，无音轨。clean 保留手机 UI、金额图形、分屏、功能卡片和原始雀鸟 Logo，便于后续剪辑加字。

已交付 `out/subtitles.ass`、`out/caption_manifest.json`、`out/audio_cue_sheet.md` 和 `out/graphics_timing.json`。音效表按实际帧号标定全部十二个钱币声、卡片入场及转折，明确标出 541–549 帧 BGM 及持续音骤停九帧，仅 541 帧的一声短“叮”保留。本项目没有生成或混入音频。

当前 FFmpeg 8.1.1 构建未包含 libass，因此采用独立 RGBA 字幕层在 FFmpeg 阶段叠加。实际使用 Noto Sans CJK SC Black / 74 px / 14 px 深色描边，文字最后一行基线精确为 y=1560，多行向上排列。所有字幕像素范围均在约定安全区内；独立 ASS 使用字体内部完整家族名 `Noto Sans CJK SC Black`。其他字幕引擎的字体度量可能不同，以交付的独立字幕 PNG 为本次已验收排版。

S06 金额图形保留 `¥ XX`，十二个月的钱币柱逐渐加快出现，金色仅用于这些金额图形。S11 从 S02/S08 的实际 90 对帧使用相同裁切和缩放，红路径每 36 帧一轮、青路径每 15 帧一轮。S13 使用实际第 1081 帧作为静态底图，三张卡片在 1081 / 1087 / 1093 帧依次弹入，6% overshoot 后归位。

S14 的 2 秒包含 1.2 秒 Logo 定格及最后 0.5 秒黑场，顺序再留出可读字幕时间会不足。因此字幕淡入与 Logo 定格段重叠：1141–1149 房屋淡出、原始雀鸟显出，1150–1185 共 36 帧 Logo 底片完全一致，1186–1200 共 15 帧纯黑。此处是明确的时序折扣。

后期图形中间帧为统一的 8-bit RGB，无损 PNG，配合独立 8-bit RGBA 字幕层；原始 16-bit RGBA Blender 序列完整保留。两版 MP4 各自从图形 PNG 编码，字幕版没有再次压缩 clean MP4。RGB→YUV 明确使用 BT.709 矩阵，并写入三项 BT.709 色彩元数据。

最终检查见 `out/gates/H/delivery_validation.json`：ffprobe 实际计数两版各 1200 帧，时长均 40 秒；两版完整 FFmpeg null decode 通过，无音频流；最后 15 帧实际解码后每个 RGB 通道最大值均为 0。`final_contact_sheet.png` 为每 50 帧抽取的 24 帧带字幕拼版，`final_storyboard_comparison.png` 为 14 镜最终代表帧与原分镜并排对照；`decoded_review/` 保存从最终 MP4 解码出的验看片。

| 文件 | 大小（bytes） | SHA-256 |
|---|---:|---|
| `out/larktun_01_clean.mp4` | 4,768,129 | `ab71795e965493fdfbdd68f928f589dbd8ab25aff4fefa9ff4fa1915fbefde26` |
| `out/larktun_01_sub.mp4` | 4,925,109 | `5eeaa47181f991401901d1520d6a822baac53a29b560c05aed963e5a79f1ae2a` |

## I · 声音（后续追加）

**阶段 I：PASS。** 用户新要求“给影片配上声音”，取代 06 文件中“不做音频”的约束。本阶段不改动已验收的画面；阶段 H 的无声成片、音效表与验收记录保持原样。完整说明见 `out/audio/SOUND_DESIGN.md`。

- **生成方式**：`film/sound_design.py` 用 NumPy 程序化合成全部音乐、音效与环境声（`audio_dsp.py`、`sound_kit.py`）。无第三方采样、外部音乐或口播，随机种子固定。共 322 个 cue，按帧放置。
- **音乐**：90 BPM，一拍 = 20 帧。冷段 D 小调：冷色 pad、玻璃动机、心跳 kick、机房风扇，S06 末尾有 riser。550 帧起转 F 大调：同一动机改为暖色电钢琴琶音加轻 lo-fi；S11 节奏起、S12 减弱为八音盒、S13 回升；S14 为 Fmaj9 长混响和弦。
- **规格执行**：
  - 541 帧前 4 ms 收尾；541–549 帧中，“叮”以外的三个分轨逐采样为 0。
  - 冷段混响、回声尾巴不跨界；550 帧暖段进入；1186–1200 帧为数字零。
  - 音效表列出的 22、64、397、12 个钱币、541、1081/1087/1093 全部实现，S11 红左青右。
- **新增细节**（音效表未列，按实际动画帧对齐）：
  - S04 红框两次脉冲，S03–S04 机房 tick 与 drone；
  - S05 锁落定 379、点击被拒 421；
  - S06 账单纸声；S07 碎片微光；
  - S08 硬盘灯 631/638/645；S09 隧道生长 721–756 与 757 接通；
  - S10 轻触、画面展开与 826/829 已连接音；
  - S11 两路到达音；S12 随 24 帧光点起伏的 hum；S14 雀鸟微光。
- **同步**：29 个关键 cue 用各自前 40 ms 波形，在交付分轨上做归一化互相关。
  - 所有音效与“叮”逐采样点落在帧起点。
  - 暖段音乐总线的 tape wow 设计上限 0.95 ms，实测最多晚 0.81 ms。
  - 两版 MP4 解码音频与母带互相关偏移 0 采样。
- **响度**：集成 −14.0 LUFS，真峰值 −1.1 dBTP，LRA 6.0 LU，限幅最大削减 1.01 dB。立体声折叠单声道损失 0.89 dB。MP4 解码后数值相同。
- **封装**：两版视频数据包与无声版逐包相同（framemd5 比较），只新增 AAC-LC 256 kbps / 48 kHz 立体声（Apple AudioToolbox 编码）。
- **证据**：`out/gates/I/audio_master_validation.json`、`audio_mux_validation.json`；频谱与分轨电平图 `soundtrack_overview.png`、`cut_541_549_zoom.png`、`ending_zoom.png`。

| 文件 | 大小（bytes） | SHA-256 |
|---|---:|---|
| `out/larktun_01_clean_audio.mp4` | 6,080,334 | `a199afaf191a057c216d1783b75bf85d6b824d6897d68dd8397aea720238b019` |
| `out/larktun_01_sub_audio.mp4` | 6,237,314 | `34209b1ed796ced61795d7dbb0db2ce13e939722fbd59b9df4fe8f21f2058de2` |
| `out/audio/larktun_01_mix.wav` | 11,520,044 | `3775c7060676dfa92bfa6a541d019d714ceea3f4976ecca71892c9fd38643645` |

## 未实现项、折扣及已知问题

- **声音（阶段 I）**：
  - 音轨为程序化合成，混音依据测量调整（逐镜响度、频谱、同步互相关），**制作过程中未经人耳试听**，审美仍需人工确认。
  - 未做口播。
  - 暖段音乐带 ≤0.95 ms 的 tape wow 延迟。
  - WAV 母带与分轨可在约 5 秒内确定性重建，不进入 Git。

- 人物使用 MakeHuman 实体人体、几何衣服和真实骨架坐姿，通过本次质量底线，未回退剪影。没有面部表演、精细发丝或布料动力学。旧版人物备注中的通勤换装、户外人物、母亲安抚动作未制作；本版按 06 的时间轴使用居家餐桌 idle 与儿童熟睡。可选猫未启用。
- C2/C3 及部分网线受到保留的完整远侧墙遮挡；三路青流在 D1 附近因间距和半径而合并发光。Gate A 的可见比例与遮挡对象已如实列出，未宣称全线无遮挡。
- 为满足灯光验收，调整了展示底板和 DUSK World，夜灯关闭阴影；S09 外隧道改为弱发光透明包络，S12 改用细弱线和单光点。具体参数与理由见 C/E，未隐去这些表现调整。
- S10 手机最底部的小号辅助说明被叙事字幕部分遮挡；设备列表、实时画面与 `18ms` 状态完整可读。S05 的开通按钮已专门补渲修正，不存在同样遮挡。
- S14 字幕淡入与 1.2 秒 Logo 定格重叠，未延长规定的 40 秒片长。原雀鸟形状与色值保留，没有重新设计。
- ASS 是独立可编辑交付，但当前 Mac 通过透明字幕 PNG 在 FFmpeg 中叠字；后期中间帧降为 8-bit，原始高位深序列仍在。复现与跨机器字体差异说明见 `film/README.md`。

没有待完成的渲染、编码或必交文件。上述限制属于本版已交付效果的明确范围。全部阶段提交保留在本地 `film/v1` 分支；大型原始/UI/后期 PNG 序列保留在工作目录，不进入 Git 历史。
